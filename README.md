# Matrix Screensaver

Clean Swift and Metal reimplementation of the installed `Matrix.saver` behavior.

Build:

```sh
make
```

Install to the current user's Screen Savers folder:

```sh
make install
```
